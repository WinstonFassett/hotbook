# Screenshots

Captured from the live Netlify deploy using Playwright. To retake:

```sh
pip install playwright
python3 -m playwright install chromium
python3 docs/screenshots/capture.py
```

Flat mode screenshots use ungrouped data. Hierarchical mode screenshots enable "group by" so the two-level structure is visible.

Retake after any significant UI change, or before updating the root README.
